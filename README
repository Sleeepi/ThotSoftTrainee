/////////////////////////
THOTSOFTTRAINEE
////////////////////////
This is a CommandLineApplication, which you can build with Maven and create an executable jar file. 
The application has 2 parameters, one is the input and the other one is the output.
Functions: 
-If you add only the source parameter, then write it to the CommandLineInterface.
(java.lang.System.out). 
-If you add the second parameter, then the applicaton copies the first file content into the other file.
/////////////////////////////////////v.0.2
with args4j parser
usable function.
-s --source
-o --output
